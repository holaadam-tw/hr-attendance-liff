# 打卡／補打卡交接紀錄（2026-08-15）

## 目前版本與部署狀態

- 程式版本：`cb5ebb2`（`main`、`origin/main`、`dev`、`origin/dev` 已同步）。
- GitHub Pages 正式站：已由 `main` 自動部署。
- 正式 Supabase 專案：`nssuisyvlrqnqfxupklb`。
- migrations 113、114、115 已在同一交易部署；migration 116 已另外套用並完成唯讀確認。
- 缺時 LINE 通知開關 `missing_work_hours_line_notifications_enabled` 必須維持關閉；不得在未人工核對名單前開啟或手動發送 LINE。

## 本次補打卡規則

1. **免打卡不是管理員身分**：只有員工資料 `no_checkin=true` 才適用免打卡，不能因為是 admin／manager 就自動排除。
2. **整日沒有任何紀錄**：免打卡人員不列入待補上、下班卡。
3. **已有單邊紀錄**：即使是免打卡人員，仍要列出缺少的另一張卡。例如 Adam 2026-08-10 已有上班 `19:52`，頁面應顯示「補下班卡」。
4. **下班時間**：預設顯示 `17:00`（下午 5:00），不再用容易誤解的 `5:00`。
5. **時鐘選取**：實際打卡時間右側的 🕒 按鈕會開啟手機原生時間選擇器；選取後同步回固定 `HH:mm` 格式。
6. **時間先後保護**：若已知上班時間晚於或等於下班補卡時間，前端會顯示紅色提示並禁止提交；主管須先更正既有上班紀錄，不能以補下班卡繞過。

## 已完成驗證

- 手機實機畫面確認：時鐘選取器正常開啟，選擇「下午 05:01」後欄位正確顯示 `17:01`。
- 補打卡導引／時間／免打卡回歸：43/43 通過。
- 相機與照片重試：22/22 通過。
- 打卡環境自我檢查：30/30 通過。
- 上班待審仍可下班：18/18 通過。
- 完整自動回歸：11 個套件全數通過。
- QA：0 FAIL；1 項既有 repo-level RLS 警告，與本次無關。
- RLS／多租戶複核：PASS；本次時鐘功能沒有新增資料庫、RPC、LINE、網路端點或排程。

## 尚待實機驗收

自動測試不可自行送出真實打卡，避免寫入正式出勤資料。下次可用 LINE／手機依序確認：

1. 進入「查詢記錄 → 補打卡」，點 2026-08-10 的「補下班卡」。
2. 點 🕒，選擇 `17:00`，確認欄位仍顯示 `17:00`。
3. 因該日上班為 `19:52`，確認系統顯示紅色衝突提示並禁止送出。
4. 使用一筆時間順序合法的測試情境，確認主管審核流程照常運作；若要真的送出，須先取得業主對該筆正式出勤寫入的明確授權。

## 操作注意事項

- 不要直接修改已在正式資料庫執行過的 migration；新修正一律新增 migration。
- 不要放寬 migration 106 的下班不得早於上班驗證。
- `get_company_daily_attendance` 必須保留公司／呼叫者驗證與 `SECURITY DEFINER` 固定 `search_path`；免打卡篩選僅能排除「上下班皆空」的資料。
- 打卡核心為 `checkin.html` 與 `common.js`；本次 B56 沒有修改這兩個檔案。

## 回復方式

- 若只有時鐘介面在特定裝置異常，採「新增小型修正」方式調整 `records.html` 的時鐘入口或回退行為；不要直接回退整個 `cb5ebb2`，因為該版本也包含 migration 116 的程式來源。
- 本次時鐘介面本身沒有資料庫變更，因此不需要資料庫回復。
- 若要回復正式資料庫的 migration 116，必須先另行盤點資料與依賴並取得明確授權，不可直接手動刪除或覆寫函式。
