## 1. 待審資料與狀態判定

- [x] 1.1 `loadTodayPendingMakeups()` 呼叫既有 RPC 時帶入目前 `company_id`
- [x] 1.2 建立可測試的今日待審上班／下班狀態 helper，兼容 `clock_in/check_in` 與 `clock_out/check_out`

## 2. 首頁按鈕與提示

- [x] 2.1 今日只有上班待審時停用上班、開放下班，顯示可先下班的白話提示
- [x] 2.2 今日下班已待審時停用兩個按鈕，避免重複送出
- [x] 2.3 保留跨日班最高優先與昨日一般班缺卡提醒，今日上班待審不得被昨日提醒關閉下班
- [x] 2.4 首頁狀態卡共用待審類型 helper，主管核准後回到正式 attendance 流程

## 3. 快取與回歸測試

- [x] 3.1 所有 `common.js` 引用同步升為 `20260811-pendingcheckout`
- [x] 3.2 新增待審上班可下班回歸測試，涵蓋正常、待審上班、待審下班、無上班、昨日缺卡與跨日班
- [x] 3.3 將新測試納入 `npm test`，同步更新 BUG_TRACKER 與架構記憶

## 4. 完整驗證與交付

- [x] 4.1 執行目標測試、`npm test`、UI、QA 與兩個 Hook，確認 0 FAIL 且無新警告
- [x] 4.2 執行 RLS／多租戶審查，確認 pending RPC 帶公司範圍且沒有新增寫入、SQL 或 schema
- [x] 4.3 提供兩種實機驗收：上班待審可下班、完全無上班申請仍不可下班
